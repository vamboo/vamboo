.component
  display flex
  flex-direction column

  & > header
    display flex
    flex-direction row

    h2
      flex 1 0
      margin 0

    a
      flex 0 1
      cursor pointer
      position relative
      top 0.5em

  & > div
    display flex
    flex-direction row

  &.hidden
    visibility hidden
